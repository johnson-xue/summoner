# Memory databases (project-specific data, generated at runtime)
memory/*.db
memory/*.db-wal
memory/*.db-shm
memory/_index.json

# Hook binaries (built via 'make build' in hooks/)
hooks/bin/*
!hooks/bin/.gitkeep

# macOS
.DS_Store

# Editor
*.swp
*.swo
*~
